# Nest backend, called only from the Next server (never the browser)
API_URL=http://localhost:4000

# must equal backend INTERNAL_PROXY_SECRET — proves requests come from this server
INTERNAL_PROXY_SECRET=
